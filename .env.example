# Static frontend mode
#
# This product does not require runtime environment variables.
# It runs entirely in the browser and can be deployed to Netlify as a static site.
#
# Add VITE_* variables here only if you later introduce public build-time settings.
